#include<iostream>

using namespace std;

int main(){
    int age = 16;
    
    cout << &age;// add & to show pointer instead, pointer is a variable that stores the address of another variable(how computer sees it(like in cheat engine))

    return 0;
}