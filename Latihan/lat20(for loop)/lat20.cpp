#include<iostream>

using namespace std;

int main(){
    for(int i = 1; i <= 5; i++){// the difference between while and for loop is that while loop is not a fixed loop but for loop is a fixed loop
        cout << i << endl;
    }
}