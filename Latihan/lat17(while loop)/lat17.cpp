#include<iostream>

using namespace std;
//looping
int main(){
    int index = 1;
    //the loop, check if the index. if index is smaller or equal to 5, then it will run the code inside the loop
    while(index <= 5){
        cout << index << endl;//print the index
        index++;//add 1 to the index every time the loop runs
    }
}